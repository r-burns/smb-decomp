include_rules

: foreach *.s |> !as |> | ../../<asm_objs>

# temporarily collect all of the external symbols into one big file
: foreach *.extern.ld |> cat %o %f > %o |> ../../all-extern-syms.ld | ../../<extern_syms>
